{{#each games}}

<div class="card" style="width: 18rem;">

    <div class="card-body">
        <h5 class="card-title">{{name}}</h5>
        <p class="card-text">This product is priced at $ {{games.price}}</p>
        <a href="#" class="btn btn-primary">more info</a>
    </div>
</div>
{{/each}}